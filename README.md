# chris-terminal-emulator
A Terminal Emulator written from scratch

´sudo apt install libfreetype-dev gettext autoreconf libtool expat` 

`cd cglm
mkdir build
cd build
cmake ..
make
sudo make install`

`cd fontconfig
./autogen.sh --sysconfdir=/etc --prefix=/usr --mandir=/usr/share/man --enable-libxml2
sudo make install`

`cd reif
./install.sh
`
